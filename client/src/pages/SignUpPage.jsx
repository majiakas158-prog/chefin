import { useState } from 'react';
import { AuthLayout } from '../components/auth/AuthLayout';
import { FormField,inputClass } from '../components/auth/FormField';
import { PasswordInput } from '../components/auth/PasswordInput';
import { RolePicker } from '../components/auth/RolePicker';
import { navigate } from '../lib/navigation';
export function SignUpPage(){const [role,setRole]=useState('chef'),[password,setPassword]=useState(''),[confirm,setConfirm]=useState('');const submit=(event)=>{event.preventDefault();if(password.length<6)return alert('Password must be at least 6 characters.');if(password!==confirm)return alert('Passwords do not match.');const fd=new FormData(event.currentTarget);localStorage.setItem('cheafinUser',JSON.stringify({fullName:fd.get('name'),email:fd.get('email'),phone:fd.get('phone'),password,role}));localStorage.setItem('selectedRole',role);alert('Account created successfully!');navigate('/signin')};return <AuthLayout><h2 className="text-3xl font-bold">Create Account</h2><p className="mt-2 text-slate-500">Join CheafIn and start your journey today.</p><h4 className="mt-7 font-semibold">Select Your Role</h4><RolePicker role={role} setRole={setRole}/><form onSubmit={submit}><FormField label="Full Name"><input className={inputClass} name="name" placeholder="Enter your full name" required/></FormField><FormField label="Email Address"><input className={inputClass} name="email" type="email" placeholder="Enter your email" required/></FormField><FormField label="Phone Number"><input className={inputClass} name="phone" type="tel" placeholder="Enter your phone number" required/></FormField><FormField label="Password"><PasswordInput value={password} setValue={setPassword} placeholder="Create password"/></FormField><FormField label="Confirm Password"><PasswordInput value={confirm} setValue={setConfirm} placeholder="Confirm password"/></FormField>{role==='chef'?<><FormField label="Years of Experience"><input className={inputClass} type="number" placeholder="e.g. 5"/></FormField><FormField label="Specialization"><input className={inputClass} placeholder="Indian, Chinese, Italian..."/></FormField><FormField label="Preferred Job Location"><input className={inputClass} placeholder="City"/></FormField></>:<><FormField label="Restaurant Name"><input className={inputClass} placeholder="Restaurant name"/></FormField><FormField label="Restaurant Address"><textarea className={inputClass} rows="3" placeholder="Restaurant address"/></FormField><FormField label="City"><input className={inputClass} placeholder="City"/></FormField></>}<label className="mb-5 block text-sm text-slate-600"><input className="mr-2 accent-brand-500" type="checkbox" required/>I agree to the Terms &amp; Conditions</label><button className="w-full rounded-xl bg-brand-500 py-3.5 font-semibold text-white transition hover:bg-brand-600">Create Account</button></form><p className="mt-6 text-center text-sm text-slate-500">Already have an account? <a className="font-semibold text-brand-500 hover:underline" href="#/signin">Sign In</a></p></AuthLayout>}
